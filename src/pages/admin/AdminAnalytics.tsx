import AdminDashboard from './AdminDashboard';

/**
 * Beta: por enquanto, "Analytics" reutiliza os mesmos dados do Dashboard.
 * Quando o backend expor relatórios mais detalhados, esta tela pode evoluir.
 */
const AdminAnalytics = () => {
  return <AdminDashboard />;
};

export default AdminAnalytics;
